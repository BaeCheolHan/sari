# Release v0.6.15 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Publish current `main` changes to GitHub and deploy a new PyPI package via GitHub Actions.

**Architecture:** Use tag-driven release flow already defined in `.github/workflows/release.yml`. Bump `src/sari/version.py`, verify lint/tests, push `main`, create/push tag `v0.6.15`, then monitor release and publish jobs until completion.

**Tech Stack:** Python, uv, pytest, ruff, Git, GitHub Actions, PyPI

---

### Task 1: Prepare release version

**Files:**
- Modify: `src/sari/version.py`
- Verify: `.github/workflows/release.yml`

**Step 1: Update version constant**
- Set `__version__ = "0.6.15"` in `src/sari/version.py`.

**Step 2: Validate release trigger path**
- Confirm release workflow triggers on pushed semver tags (`v*.*.*`) and includes PyPI publish job.

**Step 3: Commit version bump**
- Commit with message: `chore(release): bump version to 0.6.15`.

### Task 2: Verify before publish

**Files:**
- Verify: repository-wide checks

**Step 1: Run lint**
- Run `uvx ruff check .` from repo root.

**Step 2: Run tests**
- Run `pytest -q` from repo root.

**Step 3: Confirm clean status**
- Ensure `git status --short` is clean before tagging.

### Task 3: Push and trigger release workflow

**Files:**
- No file changes.

**Step 1: Push commits**
- Run `git push origin main`.

**Step 2: Tag release**
- Create tag `v0.6.15` and push tag.

**Step 3: Monitor GitHub Actions**
- Watch latest run for `release.yml` and confirm both `release` and `publish-pypi` jobs succeed.

### Task 4: Report release evidence

**Files:**
- No file changes.

**Step 1: Capture evidence**
- Record commit SHA, tag, workflow run URL/ID, and job outcomes.

**Step 2: Provide status**
- Report final deployment status and any follow-up action if a job failed.
